<!DOCTYPE html>
<html>

<%- include('header'); %>

  <body class="bg-dark">
    <%- include('navbar'); %>
    <div class="container mt-5">
       <div class="card text-center border-dark bg-secondary text-white">
          <div class="card-body">
            <img class="rounded-circle" src="<%= user.profile.picture %>">
            <p class="card-text"><%= user.profile.name %></p>
            <p class="card-text"><%= user.email %></p>
          </div>
       </div>
    </div>
  </body>
</html>
